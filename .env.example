# OpenAI API Configuration
OPENAI_API_KEY=sk-your-key-here

# Environment (development | production)
# Controls API documentation visibility
# Development: /docs, /redoc, /openapi.json are accessible
# Production: all documentation endpoints are disabled
ENVIRONMENT=development

# CORS Configuration (comma-separated list of allowed frontend origins)
# SECURITY: Restricts which domains can call the backend API
# Local development: Use localhost ports for Next.js (3000) and Vite (5173)
# Production: Use your deployed frontend URL
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173
